# Prints a string with a new line at the end
puts "I will now count my chickens:"

# Prints a string and then uses string interpolation to do math inside the string
puts "Hens #{25.0 + 30.0 / 6.0}"
puts "Roosters #{100.0 - 25.0 * 3.0 % 4.0}"

# Prints another string with a new line at the end
puts "Now I will count the eggs:"

# Does math and then prints the result to the screen
puts 3.0 + 2.0 + 1.0 - 5.0 + 4.0 % 2.0 - 1.0 / 4.0 + 6.0

# Prints another string with a new line at the end
puts "Is it true that 3.0 + 2.0 < 5.0 - 7.0?"

# Does math and then prints the result to the screen
puts 3.0 + 2.0 < 5.0 - 7.0

# Prints a string and then uses string interpolation to do math inside the string
puts "What is 3.0 + 2.0? #{3.0 + 2.0}"
puts "What is 5.0 - 7.0? #{5.0 - 7.0}"

# Prints another string with a new line at the end
puts "Oh, that's why it is false."

# Prints another string with a new line at the end
puts "How about some more."

# Prints a string and then uses string interpolation to do math inside the string
puts "Is it greater? #{5.0 > -2.0}"
puts "Is it greater or equal? #{5.0 >= -2.0}"
puts "Is it less or equal? #{5.0 <= -2.0}"
